/windows/*
# all directories at root that start with a .
# for files that don't plan on tracking
# (log/database/history files, plugins, other people's config files, etc.)
/.*/*
# automatically generated by post-install script
packages.sls
custom.el
# using a template
dunstrc
# using a template to generate
/aesthetics/.config/tridactyl/themes/custom.css
