#!/usr/bin/env bash
# to prevent the need to change in multiple places (~/.zshrc and ~/.xinitrc)
pkill -x xcape
# still deciding on
xcape -e "Control_L=BackSpace;ISO_Level5_Shift=parenleft;Shift_L=Return;ISO_Level3_Shift=Escape;Alt_L=parenleft"
