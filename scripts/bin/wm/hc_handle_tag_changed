#!/usr/bin/env bash
# hide transient dropdowns on tag switch
# may need to change if start using sticky
# not using for now; usually want to handle manually

# TODO for some reason you can't unmap windows on different tags on hlwm
# instead we have to unmap when switching back to the tag (which causes flicker)
# tdrop foreach 'herbstclient compare clients.$(printf 0x%x $wid).floating = on \
# 	  && unmap $wid'
