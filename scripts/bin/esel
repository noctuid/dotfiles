#!/usr/bin/env bash
# pipe stdin to completing-read (e.g. selectrum)

# add -s esel to everything if want dedicated daemon
# start daemon if it isn't running
# emacsclient -s esel -e 0 &> /dev/null &

# create the frame the first time and reuse afterwards
# dropdown emacsclient -c

# works better in terminal
epipe -t -e "(noct-file-filter)"
